# JWT_SECRET: Private signing password for security tokens.
JWT_SECRET="cortado_secret_key_rotation_token_2026"

# MONGODB_URI: Optional database connection string (defaults to local file-based persistent JSON store if unprovided).
MONGODB_URI="mongodb://localhost:27017/cortado"

